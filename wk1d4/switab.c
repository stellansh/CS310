#include <stdio.d>
int main(){

	char ch;
	int a,b,c;
	while((ch=getchar())!= EOF){
             switch(c)
		case '\t':
			a++; 
break
		case '\n':
			b++;
		case ' ':
			d++;
		default:
			printf("empty");
	}
	return (a+b+c);
}

		
